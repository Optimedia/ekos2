<?xml version="1.0" encoding="utf-8"?>
<s:NavigatorContent xmlns:fx="http://ns.adobe.com/mxml/2009"
					xmlns:s="library://ns.adobe.com/flex/spark"
					xmlns:mx="library://ns.adobe.com/flex/mx"
					xmlns:controller="br.com.optimedia.atendimento.controller.*"
					width="100%" height="100%" label="sda" show="init(event)">
	<s:layout>
		<s:VerticalLayout/>
	</s:layout>
	<fx:Script>
		<![CDATA[
			import br.com.optimedia.atendimento.assets.events.ListaAtendimentoEvent;
			import br.com.optimedia.atendimento.remote.MyNetConnection;
			import br.com.optimedia.atendimento.view.login.UsuarioLogado;
			
			import mx.controls.Alert;
			import mx.events.FlexEvent;
			
			[Bindable]
			private var usersObj:Object = new Object();
			
			public var usersSo:SharedObject;
			private var nc:NetConnection;
			private var i:Number = 0;
			

			protected function init(event:FlexEvent):void {
				//nc = new NetConnection();
				MyNetConnection.connect(this);
				nc = MyNetConnection.nc;
				nc.addEventListener(NetStatusEvent.NET_STATUS, onNetStatus);
				
			}	
			
			public function clickBt():void {
				var str:String = ClienteAtendimento.atendimento.cliente +"_"+ClienteAtendimento.atendimento.id_atendimento;
				usersSo.setProperty(str,"false");
				Alert.show("ok");
				
			}
			private function handleSync(event:SyncEvent):void {
				for(var user:Object in event.target.data){
					//if(!usersObj[user]){
						//usersObj[user] = event.target.data[user]
						//users.addItem({id: user, nome: event.target.data[user]});
					//}
				}
				
			}
			
			
			private function onNetStatus(e:NetStatusEvent):void{
				switch(e.info.code){
					case "NetConnection.Connect.Success":
						nc.removeEventListener(NetStatusEvent.NET_STATUS, onNetStatus);
						usersSo = SharedObject.getRemote("filaAtendimento", nc.uri,false);
						usersSo.connect(nc);
						usersSo.client = this;
						usersSo.addEventListener(SyncEvent.SYNC, handleSync);
						//clickBt();
						break;
					
					default:
						Alert.show(e.info.code.toString());
						break;
				}
			}
			/* private function onNetStatus(e:NetStatusEvent):void{
				if (e.info.code == "NetConnection.Connect.Success"){
						nc.removeEventListener(NetStatusEvent.NET_STATUS, onNetStatus);
						usersSo = SharedObject.getRemote("filaAtendimento", nc.uri,false);
						usersSo.connect(nc);
						usersSo.client = this;
			
						usersSo.addEventListener(SyncEvent.SYNC, handleSync);
						var eventLista:ListaAtendimentoEvent = new ListaAtendimentoEvent(ListaAtendimentoEvent.ADD_USER_LIST)
						this.dispatchEvent(eventLista)
						//Alert.show(e.info.code.toString());
				}else {
						Alert.show(e.info.code.toString());
					
				}
			} */

		]]>
	</fx:Script>
	<fx:Declarations>
		<!-- Place non-visual elements (e.g., services, value objects) here -->
	</fx:Declarations>
		<s:BorderContainer width="100%" height="21" backgroundColor="#CAC9C9">
			<s:HGroup width="100%" height="19" verticalAlign="middle">
				<s:Label fontWeight="bold" text="Nome: "/>
				<s:Label text="{ClienteAtendimento.atendimento.cliente}"/>
				<s:Label fontWeight="bold" text="Protocolo: "/>
				<s:Label text="{ClienteAtendimento.atendimento.protocolo}"/>
				<s:Label text="Atencente: " id="lbAtencente" visible="false" includeInLayout="false"/>
				<s:Label text="" id="txtAtendente" visible="false" includeInLayout="false"/>
				<s:Label fontWeight="bold" text="Sair"/>
			</s:HGroup>
		</s:BorderContainer>
	<s:Group width="100%" height="100%">
		<s:layout>
			<s:HorizontalLayout/>
		</s:layout>
		
	
	<s:VGroup width="250" height="100%">
		<s:Panel id="pn_atendente" width="250" height="232" title="Atendente">
			
		</s:Panel>
		<s:Panel id="pn_cliente" width="250" height="200">
			
		</s:Panel>
	</s:VGroup>
	<s:Panel id="pn_chat" width="100%" height="100%" title="Chat">
	<s:VGroup width="100%" height="100%">
		<s:TextArea id="txtHistoricoChat" width="100%" height="100%" editable="false" enabled="true"/>
		<s:HGroup width="100%" verticalAlign="middle">
			<s:Label text="Mensagem:"/>
			<s:TextInput id="txtMsn" width="100%"/>
			<s:Button label="Enviar"/>
		</s:HGroup>
	</s:VGroup>
	</s:Panel>
	</s:Group>
	<s:Button click="clickBt()" label="ok"/>
</s:NavigatorContent>
